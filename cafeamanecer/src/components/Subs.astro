---
---

<section class="mt-10 py-12">
  <div class="max-w-6xl mx-auto px-6 grid grid-cols-1 md:grid-cols-2 items-start gap-8">

    <!-- Izquierda: Solo el título -->
    <div>
        <br/>
      <h3 class="text-4xl font-bold text-gray-800">
        ¡Suscríbete a nuestras novedades!
      </h3>
    </div>

    <!-- Derecha: Texto + Formulario -->
    <div class="flex flex-col gap-4">
      <p class="text-gray-600 text-lg">
        Únete a nuestra comunidad y recibe las últimas noticias y promociones de nuestra cafetería.
        ¡No te pierdas ninguna de nuestras deliciosas ofertas!
      </p>

      <form class="flex flex-col sm:flex-row gap-4 w-full">
        <input
          type="email"
          placeholder="Tu correo electrónico"
          class="flex-1 px-4 py-3 rounded-full border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-500"
          required
        />
        <button
          type="submit"
          class="bg-black text-white px-6 py-3 rounded-full hover:bg-indigo-700 transition"
        >
          Suscribirse
        </button>
      </form>
    </div>

  </div>
</section>

